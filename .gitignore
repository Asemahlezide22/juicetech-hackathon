# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
# Ignore PyCharm's per-machine settings, but keep the shared run
# configurations so everyone gets the same Run buttons on clone.
.idea/*
!.idea/runConfigurations/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python bytecode (the backend has its own .gitignore too)
__pycache__/
*.py[cod]

# The pitch deck. Built by tools-build-deck.js — regenerate it rather than
# committing a binary that cannot be diffed or merged.
*.pptx
